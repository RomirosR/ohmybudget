# Журнал: гостевой режим (ненавязчивая авторизация)

## Шаг 1 — backend: гостевое чтение (коммит: pending)

**Дата:** 2026-06-08

**Что сделано:**
- `get_optional_user` в `api/deps/auth.py`;
- GET-роуты возвращают пустые данные без токена;
- `empty_summary()` для гостевой сводки;
- тесты `test_plans_guest_read_empty`, `test_plans_create_requires_auth`.

**Почему так:** гость может загружать экраны без 401; запись по-прежнему только с JWT.

**Как проверить:** `pytest` — guest GET 200 + [], POST 401.

## Шаг 2 — frontend: отложенное сохранение (коммит: pending)

**Дата:** 2026-06-08

**Что сделано:**
- убран полноэкранный login gate;
- `AuthModal`, `runWithAuth`, кнопки в шапке;
- guarded mutations на всех CRUD-экранах;
- баннер гостевого режима.

**Почему так:** при создании записи гостю показывается модалка; после регистрации та же мутация выполняется.

**Как проверить:** без входа добавить план → регистрация → план в списке; `npm run build`.
